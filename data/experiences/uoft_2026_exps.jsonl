{"challenge_name": "Rotor Cipher", "challenge_type": "Classical / Rotor Machine", "attack_pattern": "Cycle Conjugation / Differential Cryptanalysis", "description": "A challenge involving an Enigma-like rotor machine where the internal wirings are unknown but daily logs of message/ciphertext pairs are provided. The attack exploits verify redundancy in the permutation changes. By isolating the 'fast' rotor (which changes every step), one can compute the 'differential' permutation D_i = P_i * P_{i+1}^-1. The cycle structure of D_i reveals the conjugacy class of the fast rotor's wiring. By matching cycle structures between the observed differential and the theoretical differential (conjugation by the plugboard and other rotors), one can recover the rotor wirings up to isomorphism/phase. The attack proceeds by stripping the known plugboard, computing differentials, finding candidate rotors via cycle conjugacy, and brute-forcing the reflector/notch settings.", "solution_steps": ["Parse daily logs to obtain plaintext-ciphertext pairs.", "Compute known plugboard and setting effects to isolate the core rotor permutations.", "Calculate the differential permutation between consecutive steps (P_i * P_{i+1}^-1) to isolate the fast rotor.", "Use Cycle Conjugation analysis to match the observed differential cycle structure to candidate rotor wirings.", "Use a recursive search to solve for the rotor wiring that satisfies the conjugacy constraints.", "Repeat for days where different rotors are in the fast position to recover all rotors.", "Brute-force the remaining valid phase and notch settings to recover the Reflector."]}
{"challenge_name": "Orca", "challenge_type": "Symmetric / AES-ECB", "attack_pattern": "Permuted Output ECB Chosen Plaintext Attack", "description": "An AES-ECB oracle that allows user input but adds random padding and shuffles the output blocks. The shuffling is fixed per session. The vulnerability allows mapping the shuffled output blocks back to input positions by using 'marker' blocks. By injecting a unique pattern (Marker) at varying offsets and observing which output index produces the marker ciphertext, knowing that ECB produces identical ciphertext for identical plaintext, one can build a map of Input Block Position -> Output Index. Once mapped, the challenge reduces to a standard Byte-at-a-Time ECB decryption.", "solution_steps": ["Determine alignment/padding length by injecting repeating blocks and looking for duplicate ciphertext blocks.", "Identify a 'Marker Ciphertext' by sending a unique block pattern (e.g., 'Z'*16).", "Map Input Positions to Output Indices by placing the Marker block at offset T and finding which output index yields the Marker Ciphertext.", "Perform standard ECB Chosen Plaintext Attack (Byte-at-a-Time) using the recovered position map to query specific blocks.", "Decrypt the flag byte by byte."]}
{"challenge_name": "MAT347", "challenge_type": "ECC / ECDSA / LCG RNG", "attack_pattern": "Nonce Reuse via RNG State Attack + Custom Signature Equation exploit", "description": "An ECDSA signing service uses a custom LCG-like RNG where state evolves as cnt = cnt + 1 + h(m). The nonce k is derived as h(str(cnt)). The signature verification equation is non-standard: s = k^-1 * z + x * r. This equation allows recovering the shared secret k*Q (which is x*R) directly from the signature verification components if k is known or reused. The attack uses Lattice Reduction (LLL) to find a sequence of messages such that the sum of h(m) terms forces the RNG state to a target value (likely a collision or loop), causing Nonce Reuse between a signature and the 'exchange' action (which encrypts the flag). Using the reused k (implicitly), the shared secret can be computed from the signature and used to decrypt the exchange.", "solution_steps": ["Analyze the RNG state update function: cnt += 1 + h(m).", "Formulate a Subset Sum / Lattice problem to find a sequence of messages whose hashes sum to a target value (e.g., 0 mod 2^256 or inducing a collision).", "Use LLL to solve for the message frequencies.", "Interact with the server to reach the target RNG state.", "Obtain a signature (r, s) and then an exchange ciphertext using the (forced) same RNG state/nonce.", "Exploit the custom signature equation s = k^-1 z + x r to derive the shared secret S = x (k G) = k Q directly from r, s, z, G, Q.", "Decrypt the flag using the derived shared secret."]}
{"challenge_name": "MAT247", "challenge_type": "Finite Fields / Matrix Algebra", "attack_pattern": "Subgroup Order Distinction / DLP", "description": "A challenge requiring distinguishing between a 'generic' commuting matrix and a power of a specific matrix A over a large finite field. The flag is encoded as a sequence of bits determining the matrix type. The solution involves mapping the matrices to the field extension GF(p^12) generated by A (using A's characteristic polynomial). Since A generates a cyclic subgroup, powers of A will have an order dividing the order of A. Random commuting matrices (generating the full algebra) will likely have a larger order (dividing p^12 - 1). By checking if M^order(A) == 1, one can distinguish the two cases.", "solution_steps": ["Construct the field extension K = GF(p^12) defined by the characteristic polynomial of A.", "Map the input matrix M to an element y in K by expressing Mv as a linear combination of basis vectors A^i v.", "Compute the effective order O of the element corresponding to A (or a divisor check).", "Check if y^O == 1. If true, M is likely A^k (Bit 1). If false, M is generic (Bit 0).", "Decode the bitstream to the flag."]}
{"challenge_name": "UofT LFSR Labyrinth", "challenge_type": "Stream Cipher / LFSR", "attack_pattern": "Algebraic Attack / Annihilators / SAT Solving", "description": "A stream cipher based on a 48-bit LFSR with a nonlinear filter function. The attack uses an Algebraic Attack strategy. It finds low-degree 'annihilator' polynomials g(x) such that f(x)*g(x) = 0 (or similar for f=0 case). These annihilators allow creating low-degree equations involving the LFSR state bits for each keystream bit. The system of equations (LFSR linear updates + Annihilator constraints) is encoded into a CNF formula and solved using a SAT solver to recover the initial state.", "solution_steps": ["Analyze the nonlinear filter function (truth table).", "Compute low-degree annihilators (deg <= 3) for both outputs 0 and 1 using linear algebra (nullspace).", "Model the LFSR state bits and linear recurrence variables in SAT (CNF).", "Add clauses corresponding to the annihilator equations for each observed keystream bit.", "Run the SAT solver to find the unique initial state."]}
